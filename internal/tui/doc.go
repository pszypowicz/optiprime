// Package tui is the Bubble Tea front-end for optiprime. It owns the
// model/view/update loop, the TUI key/mouse dispatch, and the per-repo
// commands that drive scanner, ado, and gitops in the background.
package tui
